{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的能力",
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "notification:default",
    "sql:default",
    "store:default",
    "upload:default"
  ]
}
